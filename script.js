const motionOff=window.matchMedia('(prefers-reduced-motion: reduce)').matches;const sheet=document.createElement('link');sheet.rel='stylesheet';sheet.href='motion.css';document.head.appendChild(sheet);const contact=document.querySelector('#contact');if(contact)contact.addEventListener('submit',event=>{event.preventDefault();document.querySelector('#form-result').textContent='Demonstração: sua mensagem não foi enviada.';contact.reset()});if(!motionOff){const progress=document.createElement('div');progress.className='progress';progress.setAttribute('aria-hidden','true');document.body.appendChild(progress);let scheduled=false;const update=()=>{const max=document.documentElement.scrollHeight-innerHeight;progress.style.width=(max>0?Math.min(100,scrollY/max*100):0)+'%';scheduled=false};addEventListener('scroll',()=>{if(!scheduled){requestAnimationFrame(update);scheduled=true}},{passive:true});update();if('IntersectionObserver'in window){const targets=document.querySelectorAll('.features article,.ticket-grid article,.schedule article,.info>div,.questions details');document.body.classList.add('motion-ready');const observer=new IntersectionObserver(entries=>{for(const entry of entries)if(entry.isIntersecting){entry.target.classList.add('visible');observer.unobserve(entry.target)}},{threshold:.08});targets.forEach(el=>{el.classList.add('reveal');observer.observe(el)})}}